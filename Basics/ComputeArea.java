/*
A program to calculate the radius of a circle
*/

public class ComputeArea {
    public static void main(String[] args) {
        double radius = 20;
        double area = Math.PI * radius * radius;
        System.out.println("The area of the circlen of radius" + radius + " is " + area);
    }
}